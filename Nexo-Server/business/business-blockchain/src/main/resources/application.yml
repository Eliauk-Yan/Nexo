server:
  port: 8093

spring:
  cloud:
    stream:
      # 生产者配置
      bindings:
        chain-out-0: # 对应代码里的 "chain-out-0"
          content-type: application/json
          destination: chain-result-topic      # 发送到 RocketMQ 的 Topic 名称
          group: chain-group                   # 发送者所在的 Group
          binder: rocketmq                     # 指定使用 RocketMQ
  application:
    name: business-blockchain
  profiles:
    active: dev
  config:
    import:
      - classpath:/datasource.yml
      - classpath:/rpc.yml
      - classpath:/config.yml
      - classpath:/cache.yml
      - classpath:/stream.yml

nexo:
  chain:
    super:
      host: https://jmdigital.market.alicloudapi.com
      app-code: 65b1e4df85b5405d92fb427d6e44d452
      admin-pwd: B@n@n@_La15_YSJ
      admin-owner: 2
